<div class="form-group">
	<label class="control-label">Name</label>
	<%= f.text_field :name, class: "form-control" %>
	<% if campaign.errors[:name].present? %>
    	<div class="error-message"><%=  campaign.errors[:name][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Owner</label>
	<%= f.collection_select :owner, User.all, :id, :email, {}, class: "form-control" %>
	<% if campaign.errors[:owner].present? %>
    	<div class="error-message"><%=  campaign.errors[:owner][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Campaign Type</label>
	<%= f.collection_select :campaign_type_id, CampaignType.all, :id, :name, {}, class: "form-control" %>
	<% if campaign.errors[:campaign_type_id].present? %>
    	<div class="error-message"><%=  campaign.errors[:campaign_type_id][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Campaign Status</label>
	<%= f.collection_select :campaign_status_id, CampaignStatus.all, :id, :name, {}, class: "form-control" %>
	<% if campaign.errors[:campaign_status_id].present? %>
    	<div class="error-message"><%=  campaign.errors[:campaign_status_id][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Budget Cost</label>
	<%= f.text_field :budget_cost, class: "form-control" %>
	<% if campaign.errors[:budget_cost].present? %>
    	<div class="error-message"><%=  campaign.errors[:budget_cost][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Actual Cost</label>
	<%= f.text_field :actual_cost, class: "form-control" %>
	<% if campaign.errors[:actual_cost].present? %>
    	<div class="error-message"><%=  campaign.errors[:actual_cost][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Expected Revenue</label>
	<%= f.text_field :expected_revenue, class: "form-control" %>
	<% if campaign.errors[:expected_revenue].present? %>
    	<div class="error-message"><%=  campaign.errors[:expected_revenue][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Number Sent</label>
	<%= f.text_field :number_sent, class: "form-control" %>
	<% if campaign.errors[:number_sent].present? %>
    	<div class="error-message"><%=  campaign.errors[:number_sent][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Start Date</label>
	<%= f.date_select :start_date, class: "form-control" %>
	<% if campaign.errors[:start_date].present? %>
    	<div class="error-message"><%=  campaign.errors[:start_date][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">End Date</label>
	<%= f.date_select :end_date, class: "form-control" %>
	<% if campaign.errors[:start_date].present? %>
    	<div class="error-message"><%=  campaign.errors[:end_date][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<%= f.submit "Save", class: "btn btn-warning" %>
	<%= link_to "Back", campaigns_path , class: "btn btn-primary" %>
</div>
