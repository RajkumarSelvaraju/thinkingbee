<div class="container-fluid">
	<div class="row">
		<div class="col-lg-10">
			<div class="page-header">
				<h1><small>Edit Campaign</small></h1>
			</div>
		</div>
	</div>
	<div class="row">
		<div class="col-lg-offset-2 col-lg-5">
			<%= form_for @campaign do |f| %>
				<%= render 'form', :f => f, :campaign => @campaign %>
			<% end %>
		</div>
	</div>
</div>