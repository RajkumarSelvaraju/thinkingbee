<div class="container-fluid">
	<div class="row">
		<div class="col-lg-10">
			<div class="page-header">
				<h1><small>Edit Invoice</small></h1>
			</div>
		</div>
	</div>
	<div class="row">
		<div class="col-lg-offset-2 col-lg-5">
			<%= form_for @invoice do |f| %>
				<%= render 'form', :f => f, :invoice => @invoice %>
			<% end %>
		</div>
	</div>
</div>