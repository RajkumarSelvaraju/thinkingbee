<div class="container-fluid">
	<div class="row">
		<div class="col-lg-10">
			<div class="page-header">
				<h1><small>New Lead</small></h1>
			</div>
		</div>
	</div>
	<div class="row">
		<div class="col-lg-offset-2 col-lg-5">
			<%= form_for @lead do |f| %>
				<%= render 'form', :f => f, :lead => @lead %>
			<% end %>
		</div>
	</div>
</div>