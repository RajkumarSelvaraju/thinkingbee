<div class="form-group">
	<label class="control-label">First name</label>
	<%= f.text_field :f_name, class: "form-control" %>
	<% if lead.errors[:f_name].present? %>
    	<div class="error-message"><%=  lead.errors[:f_name][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Last name</label>
	<%= f.text_field :l_name, class: "form-control" %>
	<% if lead.errors[:l_name].present? %>
    	<div class="error-message"><%=  lead.errors[:l_name][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Company</label>
	<%= f.text_field :company_name, class: "form-control" %>
	<% if lead.errors[:company_name].present? %>
    	<div class="error-message"><%=  lead.errors[:company_name][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Owner</label>
	<%= f.collection_select :owner, User.all, :id, :email, {}, class: "form-control" %>
	<% if lead.errors[:owner].present? %>
    	<div class="error-message"><%=  lead.errors[:owner][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Lead Status</label>
	<%= f.collection_select :lead_status_id, LeadStatus.all, :id, :name, {}, class: "form-control" %>
	<% if lead.errors[:lead_status_id].present? %>
    	<div class="error-message"><%=  lead.errors[:lead_status_id][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Lead Source</label>
	<%= f.collection_select :lead_source_id, LeadSource.all, :id, :name, {}, class: "form-control" %>
	<% if lead.errors[:lead_source_id].present? %>
    	<div class="error-message"><%=  lead.errors[:lead_source_id][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Industry</label>
	<%= f.collection_select :industry_id, Industry.all, :id, :name, {}, class: "form-control" %>
	<% if lead.errors[:industry_id].present? %>
    	<div class="error-message"><%=  lead.errors[:industry_id][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Company</label>
	<%= f.text_field :emp_range, class: "form-control" %>
	<% if lead.errors[:emp_range].present? %>
    	<div class="error-message"><%=  lead.errors[:emp_range][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<label class="control-label">Rating</label>
	<%= f.text_field :rating_id, class: "form-control" %>
	<% if lead.errors[:rating_id].present? %>
    	<div class="error-message"><%=  lead.errors[:rating_id][0]%></div>
   	<% end %>
</div>
<div class="form-group">
	<%= f.submit "Save", class: "btn btn-warning" %>
	<%= link_to "Back", leads_path , class: "btn btn-primary" %>
</div>
